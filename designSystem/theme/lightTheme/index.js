import {
  themeColors,
  radii,
  radiiDefault,
  borderWidths,
  fontFamily,
  fontSizes,
  fontWeights
} from './base'
import buttons from './buttons'
import loaders from './loaders'
import { spacings } from '../primitives/spacing'

export default {
  // breakpoints
  colors: themeColors,
  radii,
  radiiDefault,
  borderWidths,
  fontFamily,
  fontSizes,
  fontWeights,
  space: spacings,

  // components
  buttons,
  loaders
}
